<!-- ####################### Side Bar ######################### -->
<nav class="navbar-default navbar-static-side" role="navigation">
  <div class="sidebar-collapse">
    <ul class="nav metismenu" id="side-menu">
      <li class="nav-header">
        <div class="dropdown profile-element">
          <%= image_tag current_user.picture.small.url, class: "rounded-circle", width:"40%", heigth: "40%" %>
          <a data-toggle="dropdown" class="dropdown-toggle" href="#">
            <span class="block m-t-xs font-bold">
              <%= current_user.nombre != nil ?  current_user.nombre: " " %>
              &nbsp;
              <%= current_user.apellido != nil ?  current_user.apellido: " " %>
            </span>
            <p>Rol de Usuario</p>
          </a>
        </div>
        <div class="logo-element">
          CxC
        </div>
      </li>
      <li>
        <a href="/">
          <i class="glyphicon glyphicon-th"></i>
          <span class="nav-label">
            Dashboard
          </span>
        </a>
      </li>

      <li>
        <a href="#">
          <i class="fa fa-file-text"></i>
          <span class="nav-label">
            Facturación
          </span>
          <span class="fa arrow"></span>
        </a>
        <ul class="nav nav-second-level collapse">
          <% if can? :crud, TFactura %>
            <li><%= link_to 'Factura Manual', t_facturas_path %></li>
            <li><%= link_to 'Factura Automática', t_factura_automaticas_path %></li>
          <% end %>
          <li><%= link_to 'Recibos', t_recibos_path %></li>
          <% if can? :crud, TRecargo %>
            <li><%= link_to 'Recargos', t_recargos_path %></li>
          <% end %>
          <% if can? :crud, TPeriodo %>
            <li><%= link_to 'Periodos', t_periodos_path %></li>
          <% end %>
          <% if can? :crud, TTarifa %>
            <li><%= link_to 'Tarifas', t_tarifas_path %></li>
          <% end %>
          <% if can? :crud, TTarifaServicio %>
            <li><%= link_to 'Servicios', t_tarifa_servicios_path %></li>
          <% end %>
          <% if can? :crud, TLeyenda %>
            <li><%= link_to 'Leyendas de Facturas', t_leyendas_path %></li>
          <% end %>
          <% if can? :crud, TTipoEmision %>
            <li><%= link_to 'Tipos de emisión', t_tipo_emisions_path %></li>
          <% end %>
          <% if can? :crud,TMetodoPago %>
            <li><%= link_to 'Métodos de Pago', t_metodo_pagos_path %></li>
          <% end %>
        </ul>
      </li>

      <li>
        <a href="#">
          <i class="fa fa-edit"></i>
          <span class="nav-label">
            Gestión
          </span>
          <span class="fa arrow"></span>
        </a>
        <ul class="nav nav-second-level">
          <% if can? :crud, TCliente %>
          <li><%= link_to 'Clientes', t_clientes_path %></li>
          <% end %>
          <% if can? :crud, TTipoCliente %>
          <li><%= link_to 'Tipos de Clientes', t_tipo_clientes_path %></li>
          <% end %>
          <% if can? :crud, TTipoClienteTipo %>
          <li><%= link_to 'Tipos de Clientes - Tipos', t_tipo_cliente_tipos_path %></li>
          <% end %>
          <% if can? :crud, TTipoPersona %>
          <li><%= link_to 'Tipos de Personas', t_tipo_personas_path %></li>
          <% end %>
          <% if can? :crud, TEstatus %>
          <li><%= link_to 'Estatus', t_estatuses_path %></li>
          <% end %>
          <% if can? :crud, TResolucion %>
          <li><%= link_to 'Resoluciones', t_resolucions_path %></li>
          <% end %>
          <% if can? :crud, TEmpresa %>
          <li><%= link_to 'Empresas', t_empresas_path %></li>
          <% end %>
          <% if can? :crud, TEmpresaTipoValor %>
          <li><%= link_to 'Empresas - Tipo de valores', t_empresa_tipo_valors_path %></li>
          <% end %>
          <% if can? :crud, TEmpresaSectorEconomico %>
          <li><%= link_to 'Empresas - Sectores económicos', t_empresa_sector_economicos_path %></li>
          <% end %>

        </ul>
      </li>
      <!-- -->
      <% if (can? :crud, @user ) || (can? :crud, @rol) %>
      <li>
        <a href="#">
          <i class="glyphicon glyphicon-lock"></i>
          <span class="nav-label">
            Acceso
          </span>
          <span class="fa arrow"></span>
        </a>
        <ul class="nav nav-second-level">
          <% if can? :crud, @user %>
          <li>
            <a href="/admin/users">
              Usuarios
            </a>
          </li>
          <% end %>
          <% if can? :crud, @rol %>
          <li>
            <a href="/rols/index">
              Roles de Usuario
            </a>
          </li>
          <% end %>
        </ul>
      </li>
      <% end %>
      <!-- -->
    </ul>
  </div>
</nav>
<!-- ####################### Side Bar ######################### -->
