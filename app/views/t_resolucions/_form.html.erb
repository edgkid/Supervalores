<div class="ibox-content">
  <% if no_existen_clientes_disponibles %>
  <p>No se encontraron registros de clientes disponibles.</p>
  <% else %>
  <%= form_with(model: registro, local: true) do |form| %>
  <% if registro.errors.any? %>
  <div id="error_explanation">
    <p><%= pluralize(registro.errors.count, "error") %> prohibited this registro from being saved:</p>
    <ul>
      <% registro.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
    <%= registro.inspect %>
  </div>
  <% end %>
  <div class="form-group row">
    <%= form.label :resolucion, 'Resolución', class: "col-sm-2 col-form-label text-right" %>
    <div class="col-sm-4 input-value">
      <% if editable %>
      <%= form.text_field :resolucion, autofocus: true, class: "form-control" %>
      <% else %>
      <%= registro.resolucion %>
      <% end %>
    </div>
    <%= form.label :t_estatus_id, 'Estatus', class: "col-sm-2 col-form-label text-right" %>
    <div class="col-sm-4 input-value">
      <% if editable %>
      <%= form.select :t_estatus_id, opciones_de_estatus(true, 3, true) , {}, { :class => 'form-control m-b' } %>
      <% else %>
      <%= registro.t_estatus.descripcion %>
      <% end %>
    </div>
  </div>
  <div class="form-group row">
    <%= form.label :descripcion, 'Descripción', class: "col-sm-2 col-form-label text-right" %>
    <div class="col-sm-10 input-value">
      <% if editable %>
      <%= form.text_area :descripcion, autofocus: true, autocomplete: "descripcion", class: "form-control" %>
      <% else %>
      <%= registro.descripcion %>
      <% end %>
    </div>
  </div>
  <div class="form-group row">
    <%= form.label :t_cliente_id, 'Cliente', class: "col-sm-2 col-form-label text-right" %>
    <div class="col-sm-4 input-value">
      <% if editable %>
      <%= form.select :t_cliente_id, opciones_de_clientes, {}, { :class => 'form-control m-b' } %>
      <% else %>
      <%= registro.t_cliente.razon_social %>
      <% end %>
    </div>
    <% if !editable %>
    <%= form.label :created_at, 'Fecha', class: "col-sm-2 col-form-label text-right" %>
    <div class="col-sm-4 input-value">
      <%= registro.created_at %>
    </div>
    <% end %>
  </div>
  <div class="form-group row center-content">
    <div class="col-sm-4 col-offset-sm-2">
      <%= link_to 'Volver', return_to	, {:class =>"btn btn-white btn-sm col-12"} %>
    </div>
    <% if editable %>
    <div class="col-sm-4">
      <%= form.submit 'Guardar', :class => "btn btn-primary btn-sm col-12" %>
    </div>
    <% end %>
  </div>
  <% end %>
  <% end %>
</div>