<%= fields_for registro do |form| %>
<div class="form-group row">
  <div class="col-sm-2 col-form-label opcional-prospectos opcional-persona-natural">
    <%= form.label :codigo, 'Código SERI', class: "text-right" %>
  </div>
  <div class="col-sm-4 input-value opcional-prospectos opcional-persona-natural">
    <% if editable %>
    <%= form.text_field :codigo, autofocus: true, autocomplete: "codigo", class: "form-control" %>
    <% else %>
    <%= registro.codigo %>
    <% end %>
  </div>
  <div class="col-sm-2 col-form-label">
    <%= form.label :t_estatus_id, 'Estatus', class: "text-right" %>
  </div>
  <div class="col-sm-4 input-value">
    <% if editable %>
    <%= form.select :t_estatus_id, opciones_de_estatus(true, 2, true) , {}, { :class => 'form-control m-b' } %>
    <% else %>
    <%= registro.t_estatus.descripcion %>
    <% end %>
  </div>
</div>
<% end %>