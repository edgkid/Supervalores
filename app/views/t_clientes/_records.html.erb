<div class="ibox-content">
  <%= render "shared/spiner_loading" %>
  <div class="waiting-content table-responsive">
    <% if clientes != nil && clientes.count > 0 %>
    <table class="table table-striped table-bordered table-hover dataTables clickable">
      <thead>
        <tr>
          <th>Código</th>
          <th>Razón social</th>
          <th>Teléfono</th>
          <th>Email</th>
          <th>Tipo cliente</th>
          <th>Tipo persona</th>
          <th>¿Es Prospecto?</th>
          <th>Estatus</th>
        </tr>
      </thead>
      <tbody>
        <% clientes.each do |registro| %>
        <tr data-link="<%= polymorphic_url(registro) %>">
          <td><%= registro.codigo %></td>
          <td><%= registro.razon_social %></td>
          <td><%= registro.telefono %></td>
          <td><%= registro.email %></td>
          <td><%= registro.t_tipo_cliente.descripcion %></td>
          <td><%= registro.t_tipo_persona.descripcion %></td>
          <td><%= registro.prospecto_at %></td>
          <td><%= registro.t_estatus.descripcion %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <% else %>
  <div>
    <p>No se encontraron registros.</p>
  </div>
  <% end %>
</div>